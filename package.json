{
  "scripts": {
    "start": "concurrently \"node .\" \"localtunnel --port 3000\""
  },
  "dependencies": {
    "@sendgrid/mail": "^7.4.5",
    "@slack/bolt": "^3.5.0",
    "concurrently": "^6.2.0",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "localtunnel": "^2.0.1"
  }
}
